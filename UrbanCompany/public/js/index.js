// move to login page
$("a[id='login']").click(function(){
    // now render the login page
    window.location = "/login";
});

// move to register page
$("a[id='register']").click(function(){
    // now render the register page
    window.location = "/register";
});